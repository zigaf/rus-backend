[phases.setup]
nixPkgs = ["nodejs-18_x", "npm-9_x"]

[phases.install]
cmds = ["rm -rf node_modules/.cache", "npm ci --no-cache"]

[phases.build]
cmds = ["echo 'Simple server - no build needed'"]

[start]
cmd = "npm run start"
